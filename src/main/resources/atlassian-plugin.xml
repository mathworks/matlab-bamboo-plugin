<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="matlab-bamboo-plugin" location="matlab-bamboo-plugin"/>
    
    <!-- add our web resources -->
    <web-resource key="plugin-resources" name="plugin Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        
        <resource type="download" name="plugin.css" location="/css/plugin.css"/>
        <resource type="download" name="plugin.js" location="/js/plugin.js"/>
        <resource type="download" name="images/" location="/images"/>

        <context>plugin</context>

    </web-resource>

    <!-- TASKS -->

    <!-- MATLAB Command Task -->
    <taskType key="runMATLABCommand" name="Run MATLAB Command" class="com.mathworks.ci.task.MATLABCommandTask">
        <description>Execute a MATLAB script or statement</description>
        <category name="builder"/>
        <category name="test"/>
        <configuration class="org.bamboo.plugins.configuration.MATLABCommandTaskConfigurator"/>
        <executable key="matlab" nameKey="MATLAB" />
        <resource type="freemarker" name="edit" location="/templates/editMATLABCommandTask.ftl"/>
        <!--<resource type="freemarker" name="view" location="templates/viewMATLABCommandTask.ftl"/> -->
        <!-- placeholder for task image -->
        <!-- placeholder for help link /doc -->
    </taskType>
</atlassian-plugin>
